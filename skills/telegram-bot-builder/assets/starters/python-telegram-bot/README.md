# python-telegram-bot starter

Requires Python 3.10+.

```sh
python -m venv .venv
python -m pip install -r requirements.txt
```

Set `BOT_TOKEN` in the environment. Optionally set `MENU_PHOTO` and `CUSTOM_EMOJI_ENABLED=true`. Run `python bot.py`.
