# grammY Telegram bot starter

Requires Node.js 20+.

```sh
npm install
```

Copy `.env.example` to `.env`, set `BOT_TOKEN`, and optionally set `MENU_PHOTO` and `CUSTOM_EMOJI_ENABLED=true`. Run `npm start`.
